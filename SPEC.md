# VidScribe 規格書

個人專用的影片字幕工具,參考 What'Sub(見 `spec.txt` 逐字稿)但砍掉所有商業功能,
針對單人、本機、免費使用重新設計。

## 一、定位

- **單一使用者**:自己用,不需要登入、會員、付費、額度、分享牆、客服。
- **完全本機**:影片、音訊、辨識、輸出全部在自己電腦上完成,不依賴雲端服務。
- **形式**:本機網頁工具(local web app)——後端在本機跑,瀏覽器開 `localhost` 使用。

## 二、硬體環境(已確認)

| 項目 | 規格 | 影響 |
|---|---|---|
| GPU | NVIDIA GeForce RTX 5070(12GB VRAM,Blackwell 架構) | faster-whisper large-v3 可全速跑;匯出影片可用 NVENC 硬體編碼 |
| RAM | 32GB | 足夠 |
| OS | Windows 11 | — |

> 注意:RTX 5070 是 Blackwell(sm_120),安裝 CUDA 相關套件時需確認
> ctranslate2 / PyTorch 使用支援 Blackwell 的新版本(CUDA 12.8+)。

## 三、核心功能

### 3.1 專案管理
- 首頁列出所有專案(本機資料夾即專案,不需資料庫帳號系統)。
- 新專案:拖放影片/音訊檔即建立。
- 專案內保存:原始媒體路徑、辨識結果、編輯狀態、匯出設定。自動存檔。

### 3.2 語音辨識
- 引擎:**本機 faster-whisper(large-v3)**,啟用單字級時間戳(word timestamps)。
- 流程:拖放檔案 → ffmpeg 抽出音軌 → 辨識 → 顯示進度(耗時/句數)。
- 支援影片與純音訊檔。
- 辨識時把「自訂詞庫」內容餵進 `initial_prompt`,提高人名/品牌名命中率。
- 辨識完成後自動跑:**OpenCC 簡轉繁 + 台灣用語校正**(視頻→影片 等)。

### 3.3 編輯區(完整編輯器)
操作邏輯照 What'Sub 的設計:

| 操作 | 行為 |
|---|---|
| 雙擊字幕 | 進入編輯 |
| `Enter` | 在游標處斷句(拆成兩句,時間自動依字數比例分配) |
| `Backspace`(句首)/ 刪除鍵 | 與上一句合併 |
| `Tab` / `Shift+Tab` | 跳到下一行 / 上一行 |
| 點任意句 | 影片跳到該句時間點 |
| 每句右側 ✕ | 整句刪除(此區 Backspace 語意保留給合併) |

> Alt 快速模式已確定不做:Enter 斷句/Backspace 合併已涵蓋相同能力(2026-08-19 決策)。

- **搜尋 / 取代**:搜尋字幕全文即時過濾;搜尋列的「取代」展開第二行,可把符合的字
  一次全換(純文字、區分大小寫、一次取代 = 一步復原)。取代於 2026-08-22 補上,
  和詞庫套用共用同一個取代函式。
- **詞庫(已實作,只做清單式)**:「錯誤寫法 → 正確寫法」規則取代,全域存
  `projects/_dictionary.json`;每次辨識完成自動套用,詞庫面板可手動套用到
  目前字幕(走復原系統)、可新增/刪除詞條。initial_prompt 引導與 AI 校正
  掛詞庫列為之後可加的強化,暫不實作(2026-08-19 決策)。

### 3.4 波形區
- 顯示整段音訊波形(建議用 wavesurfer.js + regions plugin)。
- **滑鼠移過即播放**(hover scrubbing),快速定位。
- 每句字幕是一個可拖拉的區塊:拖邊緣調整開始/結束時間。
- 空白處拖選 → 新增字幕;可刪除字幕。
- **Mark 點**:雙擊波形設標記點,拖字幕邊緣靠近時**磁吸**對齊。
- `B` 鍵:在播放頭位置把字幕切開。
- **切點偵測**:掃描影片畫面的剪接點(ffmpeg scene detection),在波形上
  **插旗(藍點)**;拖字幕邊緣時會磁吸到剪接點,讓字幕對齊剪輯點。

### 3.5 LLM 輔助修正(Claude Code CLI 整合)⭐
利用使用者已有的 Claude Code 訂閱,透過 headless 模式(`claude -p`)呼叫,
**不需要另外付 API 費用**。

用途:
1. **錯字/同音字修正**:Whisper 聽錯的字(ㄢㄤ不分、同音異字)整批交給 LLM 校正。
2. **台灣用語校正**:比規則式 OpenCC 更聰明的語境判斷。
3. **智慧重新斷句**:依「每句字數上限、停留時間、語意完整」重新斷句
   (影片裡講的斷句心法,交給 LLM 執行)。
4. **全文一致性**:同一人名/品牌名全文統一寫法(詞庫作為 context 餵入)。

設計原則:
- **結構化輸入輸出**:送出 JSON(編號+文字,不含時間戳),用 `--json-schema`
  強制回傳格式,程式端再驗證編號對得上才接受——LLM 永遠碰不到時間軸。
- **分批處理**:每次送 50~100 句,附前後文;顯示進度。
- **Diff 審閱,不自動套用**:LLM 的修改以逐句 diff 呈現(原文 vs 建議),
  使用者逐句或整批接受/拒絕。接受的修正照常觸發「加入詞庫?」機制。
- **優雅降級**:偵測不到 `claude` CLI 時此功能整塊隱藏,其他功能不受影響。
- **手動觸發**:按按鈕才跑(整集或選取範圍),不做即時逐字呼叫(省訂閱額度)。

**已實測確認(2026-08-19)**:`claude -p` + `--output-format json --json-schema`
可用且回傳 `structured_output` 欄位(schema 強制生效),走訂閱額度。實測數據
帶出的設計要點:

```
claude -p "<提示詞>" --output-format json --json-schema "<schema>"
→ 回傳 JSON:取 .is_error / .subtype / .structured_output
```

- **每次呼叫有固定開銷**:單次呼叫要載入 Claude Code 系統提示
  (實測 cache_creation ~28.8k tokens、單題耗時 ~17 秒)。
  → 批次要大(50~100 句/次),絕不逐句呼叫。
- **1 小時 prompt cache**:連續批次呼叫會命中快取(`ephemeral_1h`),
  同一集的多批修正接連跑最省額度。
- **模型選擇**:預設模型會用使用者 CC 設定的模型(實測是 fable-5,對錯字修正
  太貴太慢)。錯字/斷句這類任務加 `--model sonnet` 即可,更快且省訂閱額度。
- **限制工具**:純文字轉換任務不需要檔案/網路工具,呼叫時用 `--disallowedTools`
  鎖住,避免 agent 亂跑並縮短回應時間。
- **錯誤處理**:檢查 `is_error`、`subtype == "success"`、設 subprocess timeout
  (單批建議 120s),失敗的批次可重試或跳過,不擋住其他批次。

**再實測(2026-08-20)——精簡呼叫,單批 62s→16s、tokens 33k→3k**:

```
claude -p --output-format json --model sonnet \
  --system-prompt "<單行任務提示詞,要求回傳純 JSON>" \
  --exclude-dynamic-system-prompt-sections \
  --strict-mcp-config --setting-sources project --disallowedTools "*"
```

- 固定開銷的大宗是 **Claude Code 預設 agent 環境**(系統提示詞+工具定義+
  使用者的 MCP servers),`--system-prompt` 整個換掉就消失。
- **`--exclude-dynamic-system-prompt-sections` 必加**:換了系統提示詞後 CLI
  預設仍會附加動態環境段落(cwd 路徑、git 資訊),模型看到目錄名叫 VidScribe
  就把字幕裡的「What's up!」改成「VidScribe!」(實測誤傷,加旗標後消失)。
- **`--json-schema` 與精簡配置互斥**:結構化輸出靠工具呼叫、要多繞 2 個 turn,
  而 `--disallowedTools "*"` 會把它的工具也擋掉(sonnet 空回、haiku 重試 5 次
  失敗)。改成系統提示詞要求純 JSON、程式端解析、失敗重試一次。
- **系統提示詞必須壓成單行**:多行 argv 經 npm 版 claude.cmd 轉手會截斷。
- **模型**:haiku 實測比 sonnet 更慢又漏抓(23s、4/5 vs 16s、5/5),維持 sonnet。
- **關 thinking 再快 3 倍**:精簡配置後延遲主因是 thinking 的輸出量
  (同一批 out tokens 1.2k~4.6k 亂飄,生成 ~100 tok/s)。錯字校正是機械任務,
  子行程掛 `MAX_THINKING_TOKENS=0` 後單批 13~16s→4~5s、耗時穩定、品質不變。
- **不必沿用 session**:精簡後的系統提示詞前綴會跨呼叫命中 prompt cache
  (實測獨立呼叫 cache_read 相同),`--resume` 反而要拖著先前批次的對話史
  (cache_write 隨批遞增)、多一份 session 狀態要管,還汙染 `claude -r` 清單。

### 3.6 影片預覽區
- 影片播放 + 字幕即時疊加預覽。
- **字幕樣式:寫死一套好看的預設**(粗黑體 + 外框 + 陰影,置底置中),
  **不做**字型選擇、1900 字型庫、字型匯入、樣式牆、卡拉 OK 動畫。
- **字幕樣式(已實作,2026-08-20)**:專案層級兩個數值——字級倍率(0.6~2.0×)
  與距底比例(0~45%),存在 `project.json` 的 `sub_style`,舊專案沒有就用預設。
  字級同時套用到燒錄成品與直式短片;距底只管橫式成品,短片的距底由平台安全區
  決定(`clip_export.MARGIN_V_*`),不然字幕會被 Shorts UI 蓋掉。
- **逐句覆蓋(已實作,2026-08-20)**:每一句可以自己蓋掉字級與位置,存在該句的
  `style`(`scale` / `x` 水平中心 / `y` 距底),沒給的欄位沿用專案設定。位置直接
  拖曳畫面上的字幕,放開才寫回(一次拖曳 = 一筆復原);只動到的那一軸才寫成
  覆蓋,純水平拖曳不會把 y 也釘死。位移用 Dialogue 自己的 MarginL/R/V 做——
  `\pos` 會把 libass 的邊界處理一起關掉,得自己重算全部幾何;左右邊距不對稱
  就能把置中點移到 x,代價是可用寬度少 2*|dx|,一行字數跟著縮才不會爆出畫面。
  逐句覆蓋只作用在橫式成品:直式短片是裁過的畫面,同一個 x 落點完全不同。
- **預覽即成品**:疊在影片上的字幕不再是「長得像」而已——位置、字級、行高、
  斷行位置全部由 `frontend/src/subStyle.ts` 照 `exporter.to_ass` 的算法重算一次
  (含後端的整數化,以及 libass「ascent+descent = Fontsize」的字身換算),
  實測與燒錄成品差 0.2%。改任一邊的比例或斷行規則,兩邊要一起改。
- **安全框(已實作,2026-08-19 加回)**:16:9 YouTube、9:16 Shorts/Reels、4:3、
  3:4 四種預設,依影片比例自動選用、可手動切換;紅色斜紋標出平台 UI 遮擋區,
  虛線是內縮安全線,提醒字幕該換行或縮短。偏好記在瀏覽器(每專案)。

### 3.7 匯出
1. **字幕檔**:SRT / VTT → 丟回剪輯軟體。
2. **逐字稿**:純文字 / 含時間戳 Markdown。
3. **燒錄成品影片**:本機 ffmpeg + libass 把字幕燒進影片,
   用 NVENC(h264/hevc)硬體編碼加速。不用瀏覽器內運算(桌機有 GPU,ffmpeg 更快更穩)。

### 3.8 短片(已實作,2026-08-20 起;原規格沒有,後來加的)
- Claude Code CLI 讀整份逐字稿挑適合 Shorts/Reels 的片段(行號區間 + 抓力/情緒/好奇/價值
  0–10 分),伺服器驗證行號、長度與重疊;逐支預覽、微調頭尾、匯出 9:16(NVENC→x264)。
- 版型:單裁切(pan 調水平)或「拼接」(上臉下內容,YuNet 人臉偵測定位上半部);
  字幕逐字卡拉OK掃色、避開平台底部 UI。
- **人臉對位(2026-08-22)**:分析完**主動**對每支跑一次人臉偵測,把單裁切的 pan 初值
  對到臉上(不是逐幀追蹤,使用者仍可拖);臉框連範圍快取在 clip,切拼接時不必重測,
  範圍微調過才重跑。使用者決定「主動」而非多一顆按鈕。對位階段按取消 = 略過剩下的
  對位、保留選片結果(LLM 那一趟很貴)。裁切幾何集中在 `backend/clipgeo.py`,成品、
  人臉對位、前端預覽同一套。

### 3.9 日誌(2026-08-22)
- 根目錄 `vidscribe.log`(輪替 3 份),辨識/燒錄/AI/短片的開始、完成、失敗都有一行;
  終端機同步印。給非開發者回報問題用。

### 3.10 測試(2026-08-22)
- 預覽即成品的字幕幾何由 fixture 守著:後端產 `tests/sub_geometry_cases.json`,
  Python unittest 與前端 vitest 各自比對同一份;改規則要先改後端、重產、再改前端到綠。

### 3.11 視窗與鍵盤行為(2026-08-23)

原本三個右側面板(詞庫 / 短片 / AI 校正建議)固定在同一格,兩個同時開會完全疊在一起;
右下角的任務進度卡也壓在面板底部。現在:

- 面板共用一個 `.panel-dock` 底座,同時只顯示一個,其餘開著的變成上方分頁;
  非顯示中的面板用 CSS 藏起來而不是卸載,切回來時輸入到一半的內容還在。
- 面板開著時任務卡往左讓位(窄螢幕讓不開,改讓清單能捲到卡片後面)。
- 原生 `alert` / `confirm` 全面換成 `dialogs.ts` + `<DialogHost />`:
  錯誤走上方訊息卡(不擋操作、不中斷 rAF 播放頭),確認走置中對話框。
  hook 不在 React 樹裡拿 context,所以用模組層發布訂閱。
  燒錄失敗的狀態改成留在任務卡上,不再是「按掉就消失」。
- 全域快捷鍵新增焦點守門:焦點在 `select` / `summary` / 展開的選單 / 面板 / 確認框裡時不攔截
  (以前空白鍵會吃掉下拉選單的展開,Delete 會誤刪面板後面選中的字幕)。
- Esc 有了全域意義,優先序:展開中的選單 → 直式預覽 → 右側面板。
- `<details>` 選單(匯出 / AI 校正 / 字幕樣式 / 重新辨識 / 快捷鍵)點外面會自動收起、
  彼此互斥,由掛在 `App` 的 `useMenuAutoClose` 用捕捉階段的 pointerdown 統一處理。

### 3.12 其他體驗細節(2026-08-23)

- 波形跟隨播放頭改成 220ms 滑過去(`Waveform.glideTo`),不再整條瞬移;
  系統設了「減少動態效果」或距離很短仍然直接跳。
- 左右方向鍵微調播放位置:1 秒,按住 Shift 為 5 秒(`Editor.SEEK_STEP`)。
- `@media (hover: none)` 時,列刪除鈕與專案卡刪除鈕常駐顯示(以前藏在 hover 後面,觸控點不到)。
- 首頁輪詢:有上傳或有專案在跑才每 2 秒問一次,閒著時 10 秒,分頁在背景完全不問,
  切回分頁補問一次。
- 刪除專案改成「先從列表拿掉 + 5 秒內可復原」,不再事前 confirm。
  倒數中的專案會被輪詢濾掉;離開首頁時把還在倒數的刪除送出去。
  已知取捨:5 秒內直接關掉瀏覽器,那次刪除不會發生(下次再刪即可)。
- 上傳失敗可以「重試」(Upload 記住 File 與當時選的語言,之後改語言不影響重試)。

## 四、明確不做(相對 What'Sub)

- 登入 / 會員 / 付費 / 辨識額度 / 推薦機制
- 字型庫、字型匯入、字幕樣式編輯、英雄牆(樣式分享)
- 卡拉 OK 逐字動畫(橫式成品不做;直式短片後來有做,見 3.8)
- 客服機器人
- 翻譯 / 雙語字幕(**暫緩**,架構上預留欄位,之後想做再加)
- 瀏覽器內影片運算(改用本機 ffmpeg)

## 五、建議技術架構(可再討論)

```
┌─ 瀏覽器 (localhost:xxxx) ──────────────────────┐
│  前端:Vite + React(或 Svelte)                │
│  波形:wavesurfer.js(regions/markers plugin)  │
│  影片:<video> + canvas 字幕疊加                │
└──────────────┬─────────────────────────────────┘
               │ HTTP / WebSocket(進度推送)
┌──────────────┴─────────────────────────────────┐
│  後端:Python FastAPI                           │
│  ├─ ffmpeg:抽音軌、切點偵測、燒錄匯出(NVENC)│
│  ├─ faster-whisper:辨識(CUDA, word ts)       │
│  ├─ OpenCC:簡轉繁+台灣用語                    │
│  └─ subprocess → claude -p:LLM 修正            │
│  儲存:專案資料夾 + JSON 檔(不用資料庫)      │
└────────────────────────────────────────────────┘
```

## 六、開發順序建議

1. **MVP**:拖檔案 → 辨識 → 簡轉繁 → 字幕列表編輯(Enter/合併/Tab)→ 匯出 SRT
2. 波形區(顯示、拖拉時間、hover 播放)
3. Claude Code LLM 修正(diff 審閱流程)
4. 詞庫 + 搜尋取代 + Alt 快速模式
5. 切點偵測 + 磁吸 + Mark 點
6. 影片預覽疊加 + 燒錄匯出
7. (之後)翻譯/雙語

## 七、待確認事項

- [ ] 前端框架偏好(React / Svelte / 無所謂)
- [ ] 常見影片長度與檔案格式(影響進度 UX 與預設值)
- [ ] 預設字幕樣式長怎樣(可先做再調)
